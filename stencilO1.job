#!/bin/bash
#PBS -N stencilO1
#PBS -e stencilO1.error
#PBS -o stencilO1.out
#PBS -m a
#PBS -qteaching
#PBS -lnodes=1:ppn=16
#PBS -lwalltime=00:10:00

cd $PBS_O_WORKDIR

echo Running on host `hostname`
echo Time is `date`
echo Directory is `pwd`
echo PBS job ID is $PBS_JOBID

./stencil-O1 1024 1024 100
